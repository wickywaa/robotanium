import React from 'react'
import { IConnectedCockpit } from '../../models'


interface ICockpitAutoComplete {
  cockpit: IConnectedCockpit
}

export const CockpitAutoComplete:React.FC<ICockpitAutoComplete> = ({cockpit}) => {
  return (
    <div></div>
  )

}